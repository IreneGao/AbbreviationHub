<%= form_for(@abbr) do |f| %>
  <% if @abbr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@abbr.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @abbr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :abbreviation %><br>
    <%= f.text_field :abbreviation %>
  </div>
  <div class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
